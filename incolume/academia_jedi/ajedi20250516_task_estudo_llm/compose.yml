services:
  python:
    restart: unless-stopped
    image: britodfbr/estudo_llm:1.0.0
    build:
      context: ./container2
      dockerfile: Dockerfile

    volumes:
      - .:/code
  redis:
    image: "redis:alpine"
